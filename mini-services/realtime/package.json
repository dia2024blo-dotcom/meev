{
  "name": "meev-realtime",
  "version": "1.0.0",
  "private": true,
  "description": "Meev realtime mini-service — socket.io gateway (presence, DMs, servers, matchmaking, XO) on port 3003",
  "scripts": {
    "dev": "bun --hot index.ts",
    "start": "bun index.ts"
  },
  "dependencies": {
    "socket.io": "^4.8.3"
  }
}
